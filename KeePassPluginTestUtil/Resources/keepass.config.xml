<?xml version="1.0" encoding="utf-8"?>
<Configuration xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xsd="http://www.w3.org/2001/XMLSchema">
	<Meta>
		<PreferUserConfiguration>false</PreferUserConfiguration> <!-- Don't mess up user config! -->
		<OmitItemsWithDefaultValues>true</OmitItemsWithDefaultValues>
	</Meta>
  <!-- Use all defaults with a few execptions -->
  <!-- Other options can be configured per test using KeePass.Program.Config object -->
	<Application>	
		<Start>
      <!-- Don't try to do anything on startup -->
			<OpenLastFile>false</OpenLastFile> 
			<CheckForUpdate>false</CheckForUpdate> 
			<CheckForUpdateConfigured>true</CheckForUpdateConfigured>
		</Start>		
	</Application>
	<Security>
		<WorkspaceLocking />
    <Policy>
      <!-- Plugins will be manually loaded in test methods, so don't load them automatically -->
      <Plugins>false</Plugins>
    </Policy>
		<MasterPassword />
	</Security>	
</Configuration>